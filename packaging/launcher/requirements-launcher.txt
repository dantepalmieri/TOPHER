# the frozen launcher's own dependency set - deliberately excludes everything in
# the project's own requirements.txt (chromadb, sentence-transformers, claude-agent-sdk,
# fastapi/uvicorn). those only ever run through the bundled venv's python.exe as a
# subprocess, never imported directly into this process, so pyinstaller never needs
# to freeze them
pystray>=0.19.5
Pillow>=10.0.0
pywin32>=306
python-dotenv>=1.0.0
pyinstaller>=6.0.0
